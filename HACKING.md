# Hacking Guide

## Environment Variable

`MEETNOTE3_CORS=localhost:8080` to allow the request from the external host for debugging.
`MEETNOTE3_HTML_DEBUG`, `MEETNOTE3_JS_DEBUG` and `MEETNOTE3_CSS_DEBUG` to load the local file for debugging. 
